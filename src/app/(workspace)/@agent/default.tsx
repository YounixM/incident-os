import { AgentColumnSlot } from "@/components/layout/agent-column-slot";

/** Fallback when a route has no matching @agent page. */
export default function AgentSlotDefault() {
  return <AgentColumnSlot />;
}
